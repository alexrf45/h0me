---
apiVersion: v1
kind: ConfigMap
metadata:
  name: homer-waf-overrides
  namespace: homer
  labels:
    app.kubernetes.io/name: homer-waf
    app.kubernetes.io/instance: homer-waf-${ENVIRONMENT}
data:
  # Loaded last (/opt/coraza/overrides/*.conf), after the OWASP CRS rules.
  # Add SecRuleRemoveById / SecRuleUpdateTargetById exclusions here as the
  # DetectionOnly phase surfaces Homer false positives. Empty by design.
  homer-overrides.conf: |
    # Homer WAF rule exclusions — populate during detection-only tuning.
