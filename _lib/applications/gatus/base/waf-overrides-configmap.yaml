---
apiVersion: v1
kind: ConfigMap
metadata:
  name: gatus-waf-overrides
  namespace: gatus
  labels:
    app.kubernetes.io/name: gatus-waf
    app.kubernetes.io/instance: gatus-waf-${ENVIRONMENT}
data:
  # Loaded last (/opt/coraza/overrides/*.conf), after the OWASP CRS rules.
  # Add SecRuleRemoveById / SecRuleUpdateTargetById exclusions here as the
  # DetectionOnly phase surfaces Gatus false positives. Empty by design.
  gatus-overrides.conf: |
    # Gatus WAF rule exclusions — populate during detection-only tuning.
