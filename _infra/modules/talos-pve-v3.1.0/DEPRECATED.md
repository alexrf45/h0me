# DEPRECATED — scheduled for deletion

This module snapshot is superseded by `_infra/modules/talos-pve`, which is the
module `_infra/memphis` actually sources. `talos-pve-v3.1.0` is **not referenced
by any root module** and is slated for removal.

Do not add changes here. Active work (e.g. the `kubernetes_version` standalone
variable, the siderolabs/talos `0.12.0-alpha.4` migration) lands in
`_infra/modules/talos-pve` only.

See `_docs/migrations/talos-provider-0.12.0-alpha.4.md`.
